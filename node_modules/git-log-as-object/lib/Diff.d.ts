import { Commit, GitDiff } from './CommitBuilder';
export declare namespace Diff {
    const make: (commit: Commit, dir: string) => Promise<GitDiff>;
    const makeSync: (commit: Commit, dir: string) => GitDiff;
}
